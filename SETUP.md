# COSMIC PORTFOLIO — คู่มือต่อฐานข้อมูลจริง + ขึ้น Vercel

เว็บนี้เปลี่ยนจากเก็บข้อมูลใน IndexedDB (เฉพาะเบราว์เซอร์เครื่องเดียว)
มาเป็น **Supabase** ฐานข้อมูล Postgres จริงบนคลาวด์แล้ว

ทำตาม 6 ขั้นตอนนี้ครั้งเดียวจบ ใช้เวลาประมาณ 15 นาที

---

## ทำไมต้องเปลี่ยน

| | แบบเดิม (IndexedDB) | แบบใหม่ (Supabase) |
|---|---|---|
| เพิ่มผลงานจากคอม แล้วเปิดดูจากมือถือ | ไม่เห็น | เห็น |
| คนอื่นเข้าเว็บเรา | เห็นแค่ข้อมูลตัวอย่าง | เห็นผลงานจริงทั้งหมด |
| ล้าง cache เบราว์เซอร์ | ข้อมูลหายหมด | ข้อมูลยังอยู่ |
| รหัสแอดมิน | ฝังในโค้ด ใครกด View Source ก็เห็น | Supabase Auth เข้ารหัสจริง |
| รูปภาพ | base64 บวมอยู่ในฐานข้อมูล | ไฟล์จริงบน Storage โหลดเร็ว |

---

## ขั้นที่ 1 — สร้างโปรเจกต์ Supabase

1. เข้า <https://supabase.com> กด **Start your project** (ล็อกอินด้วย GitHub ได้เลย)
2. กด **New project**
   - **Name**: `cosmic-portfolio`
   - **Database Password**: ตั้งอะไรก็ได้ แต่ **จดเก็บไว้** (ใช้ตอนกู้ฐานข้อมูล ไม่ใช่รหัสล็อกอินเว็บ)
   - **Region**: `Southeast Asia (Singapore)` — ใกล้ไทยที่สุด เว็บจะเร็วกว่า
3. รอสร้างเสร็จประมาณ 2 นาที

> แผนฟรีให้ 500 MB database + 1 GB storage ไม่ต้องผูกบัตรเครดิต

---

## ขั้นที่ 2 — สร้างตารางและระบบสิทธิ์

1. ในโปรเจกต์ Supabase เลือกเมนูซ้าย **SQL Editor** → **New query**
2. เปิดไฟล์ `supabase-setup.sql` ในโฟลเดอร์นี้ **คัดลอกทั้งไฟล์** ไปวาง
3. กด **Run** (หรือ `Ctrl + Enter`)
4. ขึ้น `Success. No rows returned` = เรียบร้อย

ไฟล์นี้จะสร้างให้อัตโนมัติ

- ตาราง `projects`, `certificates`, `activities`
- **RLS (Row Level Security)** — ใครก็อ่านได้ แต่เขียนได้เฉพาะคนที่ล็อกอิน
- Storage bucket ชื่อ `portfolio-media` สำหรับเก็บรูป
- Realtime — หน้าเว็บอัปเดตเองทันทีที่แอดมินกดบันทึก

รันซ้ำกี่รอบก็ได้ ไม่พัง

---

## ขั้นที่ 3 — สร้างบัญชีแอดมิน

1. เมนูซ้าย **Authentication** → **Users** → **Add user** → **Create new user**
2. ใส่อีเมลกับรหัสผ่านที่จะใช้ล็อกอินหน้าแอดมิน
3. **ติ๊ก `Auto Confirm User`** ← สำคัญ ไม่ติ๊กจะล็อกอินไม่ได้เพราะยังไม่ยืนยันอีเมล
4. กด **Create user**

### ⚠️ แล้วปิดการสมัครสมาชิกทันที

**Authentication** → **Sign In / Providers** → **Email** → ปิด **Allow new users to sign up** → **Save**

ถ้าไม่ปิด ใครก็สมัครบัญชีเองแล้วได้สิทธิ์เขียนข้อมูลเราทันที เพราะ RLS ตั้งไว้ว่า
"คนที่ล็อกอินแล้วเขียนได้" — ขั้นนี้ห้ามข้าม

> อยากล็อกแน่นกว่านี้ ให้เปิดหัวข้อ 8 ท้ายไฟล์ `supabase-setup.sql`
> จะจำกัดสิทธิ์เขียนไว้ที่อีเมลของเราคนเดียวเท่านั้น

---

## ขั้นที่ 4 — ใส่ key ลงในเว็บ

1. **Project Settings** (รูปเฟือง) → **API Keys**
2. คัดลอก 2 ค่า
   - **Project URL** — เช่น `https://abcdefghijkl.supabase.co`
     (ถ้าหาไม่เจอในหน้านี้ ดูที่ **Settings → General** หรือกดปุ่ม **Connect** บนแถบบนสุด)
   - **คีย์ฝั่งหน้าเว็บ** — โปรเจกต์ของคุณจะมีแบบใดแบบหนึ่ง ใช้ได้ทั้งคู่

| แบบ | หน้าตา | หาได้ที่ |
|---|---|---|
| ใหม่ (ค่าเริ่มต้นตอนนี้) | `sb_publishable_xxxxxxxx` | **Settings → API Keys** ช่อง **Publishable key** |
| เก่า | `eyJhbGciOiJIUzI1NiI...` | หน้าเดียวกัน แถบ/หัวข้อ **Legacy API keys** ช่อง **anon** `public` |

> **หา `eyJ...` ไม่เจอเป็นเรื่องปกติ** — Supabase เปลี่ยนมาออก `sb_publishable_...`
> ให้โปรเจกต์ใหม่แทนแล้ว ใช้อันนั้นได้เลย ไม่ต้องไปตามหาคีย์แบบเก่า
> (ถ้าอยากได้จริง ๆ มันอยู่ใต้หัวข้อ Legacy API keys อาจต้องกดขยายก่อน)

3. เปิดไฟล์ `assets/supabase-config.js` แล้วแทนที่ 2 บรรทัด

```js
window.COSMIC_SUPABASE_CONFIG = {
  url:     'https://abcdefghijkl.supabase.co',
  anonKey: 'sb_publishable_AbCdEf1234567890',   // หรือ 'eyJhbGciOiJIUzI1NiI...'
  bucket:  'portfolio-media'
};
```

### คีย์นี้เปิดเผยได้จริงหรือ

จริง ทั้ง `sb_publishable_...` และ `anon` key ถูกออกแบบมาให้ฝังในหน้าเว็บ
สิ่งที่ป้องกันข้อมูลคือ RLS ไม่ใช่การซ่อน key

**แต่ `sb_secret_...` กับ `service_role` ห้ามเอามาใส่เด็ดขาด** สองอันนั้นข้าม RLS ทุกข้อ
ใช้เฉพาะฝั่งเซิร์ฟเวอร์เท่านั้น — ถ้าเผลอใส่ โค้ดจะปฏิเสธและขึ้นคำเตือนสีแดงใน Console ให้

---

## ขั้นที่ 5 — ย้ายข้อมูลเดิมขึ้นคลาวด์

ข้อมูลที่เคยเพิ่มไว้ยังอยู่ใน IndexedDB ของเบราว์เซอร์เครื่องเดิม ย้ายขึ้นได้ในคลิกเดียว

1. เปิด `admin.html` **ในเบราว์เซอร์เครื่องที่มีข้อมูลเดิมอยู่**
2. ล็อกอินด้วยอีเมล/รหัสผ่านจากขั้นที่ 3
3. ดูมุมขวาบน ต้องขึ้นป้ายเขียว **CLOUD** (ถ้าขึ้นป้ายส้ม LOCAL ONLY แปลว่า key ยังไม่ถูก)
4. กดปุ่มเขียว **นำเข้าข้อมูลเดิม**

ระบบจะย้ายผลงาน เกียรติบัตร กิจกรรม ขึ้นฐานข้อมูล และ **อัปโหลดรูป base64 ขึ้น Storage
แล้วเปลี่ยนเป็นลิงก์จริงให้อัตโนมัติ**

> ถ้าไม่มีข้อมูลเดิม หรืออยากเริ่มใหม่หมด ก็ข้ามขั้นนี้แล้วกด **LAUNCH NEW MISSION** เพิ่มเองได้เลย

---

## ขั้นที่ 6 — Deploy ขึ้น Vercel

### ผ่าน GitHub (แนะนำ — แก้โค้ดแล้ว push เว็บอัปเดตเอง)

```bash
git add .
git commit -m "เชื่อมต่อ Supabase แทน IndexedDB"
git push
```

1. เข้า <https://vercel.com> ล็อกอินด้วย GitHub
2. **Add New** → **Project** → เลือก repo นี้
3. Framework Preset ปล่อยเป็น **Other**, Build Command และ Output Directory **เว้นว่างไว้**
4. กด **Deploy**

เสร็จแล้วจะได้ลิงก์ `https://ชื่อโปรเจกต์.vercel.app`
หน้าแอดมินอยู่ที่ `https://ชื่อโปรเจกต์.vercel.app/admin`

### ผ่าน CLI

```bash
npm i -g vercel
vercel --prod
```

ไม่ต้องตั้ง Environment Variables ใด ๆ เพราะ key อยู่ในไฟล์ `assets/supabase-config.js` ที่ deploy ไปด้วยแล้ว

---

## โครงสร้างไฟล์

```
Portfolio DEV/
├── index.html                  หน้าเว็บสาธารณะ (อ่านข้อมูลอย่างเดียว)
├── admin.html                  หน้าแอดมิน (ต้องล็อกอินจึงเขียนได้)
├── assets/
│   ├── supabase-config.js      ★ ใส่ URL + anon key ที่นี่
│   └── cosmic-db.js            ชั้นเชื่อมฐานข้อมูล (ไม่ต้องแก้)
├── supabase-setup.sql          รันใน Supabase SQL Editor ครั้งเดียว
├── vercel.json                 ตั้งค่า deploy
└── SETUP.md                    ไฟล์นี้
```

---

## โหมด LOCAL กับ CLOUD

`assets/cosmic-db.js` ตรวจ config ให้อัตโนมัติ

- **ยังไม่ใส่ key** → โหมด LOCAL ใช้ IndexedDB แบบเดิม เปิดเว็บทำงานได้ปกติ
  ล็อกอินแอดมินด้วยรหัสเดิม `210148`
- **ใส่ key แล้ว** → โหมด CLOUD ใช้ Supabase ล็อกอินด้วยอีเมล/รหัสผ่าน

เช็คโหมดปัจจุบันได้ที่ Console ของเบราว์เซอร์ (`F12`) จะมีบรรทัด `[CosmicDB] โหมด: ...`

---

## แก้ปัญหาที่เจอบ่อย

| อาการ | สาเหตุ / วิธีแก้ |
|---|---|
| หา `anon public` / `eyJ...` ไม่เจอ | ปกติ — โปรเจกต์ใหม่ใช้ `sb_publishable_...` แทนแล้ว ที่ **Settings → API Keys** ใช้อันนั้นได้เลย |
| ขึ้นป้ายส้ม LOCAL ONLY ทั้งที่ใส่ key แล้ว | คีย์ต้องขึ้นต้นด้วย `sb_publishable_` หรือ `eyJ` และ URL ต้องเป็น `https://...` เต็ม ๆ เปิด Console (`F12`) ดูข้อความ error |
| Console ขึ้นคำเตือนสีแดงว่าใส่ SECRET KEY | คุณหยิบ `sb_secret_...` หรือ `service_role` มา ให้เปลี่ยนเป็น Publishable/anon แล้ว **Revoke คีย์ที่หลุดทิ้งทันที** |
| `อีเมลหรือรหัสผ่านไม่ถูกต้อง` | สร้าง user ในขั้นที่ 3 แล้วหรือยัง และติ๊ก Auto Confirm User หรือเปล่า |
| `ยังไม่ได้ยืนยันอีเมล` | Authentication → Users → กดที่ user → Confirm email |
| `Email logins are disabled` | เผลอปิดสวิตช์ใหญ่ของ Email provider ไปด้วย — เปิด **Enable Email provider** กลับ แล้วปิดเฉพาะ **Allow new users to sign up** |
| `ไม่มีสิทธิ์เขียนข้อมูล (RLS ปฏิเสธ)` | เซสชันหมดอายุ ให้ล็อกเอาต์แล้วล็อกอินใหม่ |
| `ไม่พบ bucket portfolio-media` | ยังรัน `supabase-setup.sql` ไม่ครบ รันซ้ำอีกรอบ |
| เว็บว่างเปล่าไม่มีผลงาน | ปกติ — ตารางยังว่าง ให้กด "นำเข้าข้อมูลเดิม" หรือเพิ่มผลงานใหม่ |
| แก้ key แล้วเว็บยังใช้ค่าเดิม | กด `Ctrl + Shift + R` ล้าง cache |
| เปิดไฟล์ด้วยการดับเบิลคลิก แล้ว Supabase ไม่ทำงาน | ต้องเปิดผ่าน http ไม่ใช่ `file://` — รัน `python -m http.server 5500` แล้วเข้า <http://localhost:5500> |

---

## หมวดหมู่ผลงาน

ช่อง **หมวดหมู่ระบบ** ในฟอร์มเพิ่มผลงานเป็นแบบเลือกได้/พิมพ์เองได้ มีให้เลือก 12 หมวด

`AI & Vision` · `Web Platform` · `Mobile Application` · `Desktop Application` ·
`Database & Backend` · `Data & Analytics` · `Algo & Trading` · `Cybersecurity & Network` ·
`IoT & Embedded` · `Game & Interactive` · `Design & Multimedia` · `Tools & Cloud`

พิมพ์ชื่อหมวดใหม่ที่ไม่มีในรายการได้เลย ระบบจะจำไว้และเพิ่มเข้าทั้งรายการเลือกและตัวกรองให้เอง
ส่วนหน้าเว็บสาธารณะจะสร้างปุ่มกรองจากหมวดที่มีผลงานจริงเท่านั้น หมวดที่ยังไม่มีผลงานจะไม่ขึ้น

อยากแก้รายการมาตรฐาน (เพิ่ม/ลบ/เปลี่ยนคำอธิบายไทย) แก้ที่ตัวแปร `CATEGORIES`
ในไฟล์ `assets/cosmic-db.js` ที่เดียว ทั้งฟอร์มและตัวกรองจะอัปเดตตาม

---

## หลังจากนี้

- เพิ่ม/แก้/ลบ ผลงานผ่าน `admin.html` ได้เลย หน้าเว็บสาธารณะอัปเดตทันทีไม่ต้อง deploy ใหม่
- ส่วน PROJECT GRAPH แชร์ลิงก์เจาะจงได้ เช่น `?cat=AI %26 Vision` หรือ `?q=yolov8&sort=az`
  เปิดลิงก์แล้วหน้าเว็บจะคืนสถานะตัวกรอง คำค้น และการเรียงลำดับให้เอง
- กด **Backup JSON** เก็บไฟล์สำรองไว้เป็นระยะ
- ดูข้อมูลดิบได้ที่ Supabase → **Table Editor**
- ดูรูปที่อัปโหลดได้ที่ Supabase → **Storage** → `portfolio-media`
